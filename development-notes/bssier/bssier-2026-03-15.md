# Дата: 2026-03-15

- **Что было сделано:** Добавлена страница с пользователями для админа, админ может просмотреть всех юзеров и редактировать их поля.
- **Мысли / Планы:** Далее перейду к созданию страницы с заказами.
- **Разбор кода тиммейта:** Просмотрел pr моего тиммейта Rorodeathless1 и после просмотра кода оставляю 3 комментария.
1) Роман создавал catalog page. Первое что мне очень понравилось это присутсвие констант
   Это мы можем увидеть в файле constants.tsx:
```javascript
export const TEXT_SELECT = {
  CATEGORIES: 'categories',
};
```

А здесь уже его применение 

```javascript
<InputLabel id="demo-simple-select-label" sx={LABEL_STYLE}>
        {TEXT_SELECT.CATEGORIES}
</InputLabel>
```
Я считаю что подход который выбрал Роман является одним из лучших тк если мы захотим что-то поменять то не будем искать это в коде а просто изменим константу

2) Также интересным решением является логика в в SelectComponent которая предотвращает конфликт когда выбрано все и что-то еще
``` javascript 
   if (
   (value?.includes('all') && !selectedCategories.includes('all')) ||
   !value.length
   ) {
   setSelectedCategories(['all']);
   return;
   }
   setSelectedCategories(value.filter((item) => item !== 'all'));
```

3) Одним из лучших решений Романа я считаю навешивание debouce на инпут для того чтобы не отправлять запрос постоянно когда мы вводим поиск

```javascript
 useEffect(() => {
  const timer = setTimeout(() => {
    getData();
  }, 1000);
  return () => clearTimeout(timer);
}, [search]);
```

- **Затраченное время:** 4ч.
# Дата: 2026-03-16

## Code Review

- [PR #54: add catalog page](https://github.com/MegaRostBLR1/eCommerceLegendaryFrontendDevelopers/pull/54/changes#r2941914412) - 1 комментарий (функция getData используется внутри useEffect, а в dependency array ([]) её нет, предложила добавить её в зависимости)

- [PR #54: add catalog page](https://github.com/MegaRostBLR1/eCommerceLegendaryFrontendDevelopers/pull/54/changes/BASE..f764d19387652437a1aad4237f376de433fc6bfb#r2941971678) - 1 комментарий (предложила добавить try/catch для fetch запроса)

- [PR #68, add users page for admin add diary and tests](https://github.com/MegaRostBLR1/eCommerceLegendaryFrontendDevelopers/pull/68/changes#r2942050454) - 1 комментарий (предложила вынести логику доступа для админа в модуль для повторного использования)

- [PR #68, add users page for admin add diary and tests](https://github.com/MegaRostBLR1/eCommerceLegendaryFrontendDevelopers/pull/68/changes#r2942081537) - 1 комментарий (предложила тест для смены страницы, чтобы убедиться что метод loadUsers вызывается с обновленной страницей)

- **Что было сделано:** я написал AIGenerationPage в ней
  поле для ввода промпта админом
  и кнопка отправить промпт.
  Это просто визуал пока, логику уже другой член команды будет делать.
  Я принимаю json от ии и превращаю его в объекты (в файле ai-mapper)
  эти объекты превращаю в карточки-черновики и вывожу сеткой на этой же странице.
  Над ними висит кнопка отправить всё на сервер (handleSaveAll использует Promise.all и userService.createService).
  Помимо этого при клике на иконку редактирования всплывает модалка
  в ней админ может редактировать все поля
  и сохранять (временно) одной кнопкой (handleConfirm -> вызывает onLocalUpdate из пропсов)
  или же сразу отправлять на сервер другой кнопкой (она тоже в модалке).
  ещё на каждой карточке есть иконка удаления - этот объект удаляется из временного массива черновиков которые ии прислал. (Проп onDelete внутри мапа, вызывает setDrafts с фильтрацией)
  Старалась переиспользовать компоненты (карточки, модалки)
- **Проблемы, Решения (или попытки):** У меня была целая война с ts и его типами. В интерфейсе Service категории - это массив объектов. А бэкенд ждал массив чисел: categories: number[]. Когда данные приходили из модалки (values.categories), это number[].
  Но чтобы карточка не сломалась пришлось в onLocalUpdate превращать эти id обратно в объекты. Написала функцию formatForBackend, которая перед отправкой на сервер чистит объекты и только id оставляет.
  Ещё когда я передавала в категории объект { id: 1, name: '' }. Это валидный объект Category, но визуально это ломало карточку, она рисовала пустую плашку. Добавила пропс availableCategories в модалку и методом .find() внутри маппера по id доставала из справочника объект, который соответствует типу.
  Вообще сложностей было много и времени заняло много, и я всё в дневнике описать и не смогу.
- **Мысли / Планы:**
- **Затраченное время:** 14 часов где-то, много ушло времени чтобы придумать что вообще делать и как. Разные идеи были, но не все были технически верными.

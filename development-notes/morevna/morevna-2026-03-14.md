# Дата: 2026-03-14

- **Что было сделано, Проблемы:** опять нашла баг в своей прошлой таске (он возник при мерже мой ветки в девелопмент). Разобралась с ним. 
Он состоял из двух частей: при нажатии на иконку редактирования карточки был белый экран. 
И вторая: при нажатии на иконку удалить, ничего не происходило. Была ошибка 403 Forbidden. Я провела отладку через Chrome DevTools (Network), убедилась что JWT-токен отправляется в заголовке запроса и роль 'admin' в Payload тоже есть. Я думаю что проблема на стороне бэкенда и поэтому сообщила о ней ответственному разработчику"
- **Решения (или попытки):** 
добавила проверку if (!selected) чтобы метод .includes() у пустоты не вызывался и React не падал. 
Синхронизировала пропсы. В ServicesPage я ожидала selectedCategories, а в select-component использовалось имя category.
Поправила блок с SelectComponent. Он теперь принимаем новые значения и конвертируем их обратно в числа.
Мусор почистила. Удалила неиспользуемые импорты (Box, Chip) и лишние интерфейсы, на которые ругался линтер.
- **Затраченное время:** около 90 минут. Искать ошибки часто дольше чем писать код заново.
